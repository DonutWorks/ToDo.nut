<%= form_for Comment.new, url:project_history_comments_path(@project.id, @history.phistory_id) do |f| %>
<p>
	<%= f.label '댓글달기' %><br>
	<%= f.text_area :contents, :class => 'form-control' %>
</p>
<p>
	<%= f.submit :class => 'btn btn-default' %>
</p>
<% end %>